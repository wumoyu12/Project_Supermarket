def Checknum(num):
    length=len(num)
    if (length == 0 or num == "0"):
        ifcorrect="false"
    else:
        eachnum=[]
        eachnum.extend(num)
        for i in range (0, length):
            if ()
